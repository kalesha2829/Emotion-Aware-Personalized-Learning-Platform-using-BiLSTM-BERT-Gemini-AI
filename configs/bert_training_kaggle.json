{
  "model_name": "bert-base-uncased",
  "batch_size": 32,
  "eval_batch_size": 64,
  "max_length": 128,
  "epochs": 3,
  "learning_rate": 2e-5,
  "weight_decay": 0.01,
  "warmup_ratio": 0.1,
  "early_stopping_patience": 2,
  "fp16": true,
  "save_dir": "models/bert_emotion",
  "checkpoint_dir": "models/bert_checkpoints"
}
